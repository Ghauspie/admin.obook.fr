<form method="post" action="{{ path('apotheose_delete', {'id': apotheose.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ apotheose.id) }}">
    <button class="btn">Delete</button>
</form>
