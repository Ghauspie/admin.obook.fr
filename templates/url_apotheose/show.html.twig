{% extends 'base.html.twig' %}

{% block title %}UrlApotheose{% endblock %}

{% block body %}
    <h1>UrlApotheose</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ url_apotheose.id }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ url_apotheose.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('url_apotheose_index') }}">back to list</a>

    <a href="{{ path('url_apotheose_edit', {'id': url_apotheose.id}) }}">edit</a>

    {{ include('url_apotheose/_delete_form.html.twig') }}
{% endblock %}
