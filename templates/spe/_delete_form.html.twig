<form method="post" action="{{ path('spe_delete', {'id': spe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ spe.id) }}">
    <button class="btn">Delete</button>
</form>
