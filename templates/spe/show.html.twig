{% extends 'base.html.twig' %}

{% block title %}Spe{% endblock %}

{% block body %}
    <h1>Spe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ spe.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ spe.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('spe_index') }}">back to list</a>

    <a href="{{ path('spe_edit', {'id': spe.id}) }}">edit</a>

    {{ include('spe/_delete_form.html.twig') }}
{% endblock %}
